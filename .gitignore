*__pycache__
*__pycache__/*
*pyc
__pychace__/*

# Webapp runtime files
.snakemake_run.json
*.log
.config_backups/
.sessions/
*.cpython

# User-specific config (use config.template.yaml as starting point)
config.yaml

# Local virtual environments (e.g. scripts/uniprot_fetch/.venv)
.venv/
**/.venv/

# Snakemake state dir (now lives inside the output dir; ignore everywhere)
.snakemake/
**/.snakemake/
